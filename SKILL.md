---
name: session-handoff-guard
description: 管理长对话的换窗提醒、精简交接和新主控接管。用于用户提出上下文混乱、换窗口、会话交接或启用持续上下文检查；支持主控与执行双窗口，保持目标、授权及费用计数连续。不是普通摘要或业务执行技能。
---

# 会话交接守护

使新窗口继承已确认目标和可信进度，而不是继承全部聊天与试错。默认使用用户当前语言。自动发现本技能不等于自动创建窗口、发送工单或启动业务。

## 判断与提醒

- 用户启用持续检查后，在工单交接、阶段结束及明显纠错时评估上下文健康；不建立后台轮询或自动化，不承诺空闲时持续监控。
- 关注：重复询问已确认事项、混淆历史与当前状态、遗漏授权边界、把讨论当决定、反复重读大段历史才能回答。不要仅按轮数或主观疲劳判定。
- 只有宿主提供可信上下文用量时才报告数字；账户使用额度不等于上下文占用。没有数据就明确未知，不编造百分比或统一阈值。
- 有具体风险时建议在当前工单结束后换窗，并简述依据。没有风险时保持安静，不每轮附加健康报告。
- 若已经出现事实混淆，暂停新工单派发，定点核对并更正后准备交接。不要擅自中断执行窗口已经获准的任务。

## 事实与目标

- 用户明确批准的需求决定目标；代码、运行记录和原始结果证明实现现状。代码不能覆盖未实现的需求。
- 区分：用户已确认决定、已核实事实、执行者报告、待验证项、未批准建议。记录关键结论的来源及时间。
- 新旧内容冲突时注明冲突及依据；不把最新助手总结自动视为真源。不把暂时优先顺序写成取消长期目标。
- 保留目标版本或来源、当前工单、批准范围、停止条件、未完成项、运行边界、累计付费尝试与不确定提交。未知计数不得记为零，换窗不得重置额度。
- 本技能不固定产品名称、端口、模型或限额；从该项目当前已确认记录读取。不凭旧快照称当前运行已核实。

## 双窗口信息边界

- 主控只接收每工单一份短交接单；日志、试错、堆栈和大段代码留在执行窗口或证据文件。遇到需扩大范围、超授权或无法继续的阻塞才提前报告，无新事实不重复通知。
- 主控按需核查代码差异和原始测试/部署/结果证据，不默认通读执行窗口全部历史。报告标记“已核实”或“尚未验证”，不猜测执行者动机。
- 真实性核查不等于完整产品验收；保持独立验收职责和状态，不把部署或测试通过等同于产品闭环。
- 工单必须先展示给用户核对，用户明确批准派发后才能发送。完成通知、认可建议或批准换窗，都不授权下一批业务工单。

## 准备交接

用户要求准备交接时，读取 [交接模板](references/handoff-template.md)。先完成可审阅的交接记录与接管提示词，再询问是否创建/切换窗口；已明确授权本次创建则不重复询问。

1. 优先采用项目既有交接位置与记录结构；无约定时使用 `<项目根目录>/.handoff/<时间戳>-<角色>.md`。如果用户只要聊天文本，就不写文件。非项目任务使用获准的输出位置。不得改写治理文件或写入个人长期记忆来代替交接。
2. 只读取支持关键结论的最小证据。维持增量记录，不无限嵌套旧总结。交接保留必要状态与证据索引，不搬运完整聊天。无关敏感信息和任何凭据不进入交接。
3. 交接正文通常控制在约 1200 个汉字以内；必要审计标识可保留，详细证据通过路径引用。不能为凑长度省略授权、未知提交或未完成需求。
4. 保存为独立快照，不覆盖并行窗口的记录；记录准备时点。若执行仍在运行，注明工单归属和证据截止时间，新窗口必须核对之后的增量，不能重复提交。
5. 提供简短的接管提示词，指定项目、交接文件、角色、执行窗口标识及“先只读核对，不自动开工”。让用户审阅目标、进度和下一步是否准确。

## 创建与切换

- 只有用户明确要求创建新窗口/任务才使用宿主创建工具。“准备交接”“是否该换窗”“安装此技能”不构成创建授权。
- 用户自己开窗口时，只提供可复制提示词与文件路径。
- 有创建工具时遵守其当前规则并核实项目身份；不臆造项目 ID、分支或任务 ID，不自行覆盖模型设置。新任务的初始消息就是上述只读接管提示词。
- 新窗口只接管协调时，明确需要访问原工作目录与已有改动，按工具要求说明并选择适合的环境；若默认隔离环境不含这些改动，不声称它继承了当前工作树。
- 创建成功不等于接管完成。新窗口返回接管摘要、用户确认其成为主控后，记录新旧主控标识与切换时间；旧主控停止派发，新主控成为唯一派发者。
- 仅在用户授权的范围内通知执行窗口更新汇报对象。普通执行报告不触发自动派发。不要自动归档、关闭或删除旧窗口及交接记录。
- 工具不可用或交接文件不可访问时说明限制并给可复制文本，不声称已创建或已同步。

## 新窗口恢复

先读取交接记录、适用治理和目标真源；仅按需读取关联证据。先报告：目标、当前状态、在途工单、未决项和权限边界。

定点验证五件事：
1. 用户真正要交付什么，而非当前代码只能做什么？
2. 哪些完成有证据，哪些只是执行者报告？
3. 是否有仍运行的工单或提交不确定的付费请求？
4. 下一步哪些已获准，哪些还需要用户确认？
5. 当前唯一主控是谁，交接快照之后发生了什么？

有缺口就具体指出，不重问全部历史、不猜测、不因恢复会话自动重试请求。新业务工单仍遵守先向用户展示、确认后派发。
